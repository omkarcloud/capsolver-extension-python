{
  "scripts": {
    "upload": "rm -rf dist/ capsolver_extension_python.egg-info  MANIFEST && python setup.py sdist && pip install -e . && python -m twine upload dist/* --verbose",
    "install": "rm -rf dist/ capsolver_extension_python.egg-info  MANIFEST && python setup.py sdist && pip install -e .",
    "local-install": "pip install ."
  }
}